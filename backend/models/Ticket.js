const mongoose=require('mongoose')

const TicketSchema=new mongoose.Schema({
    booked_user:{
        type:,
        required:true
    },
    Show:{
        type:,
        required:true

        
    }
});

